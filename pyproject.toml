[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-tile-feedback"
version = "0.1.0"
description = "Tile quality feedback — ratings, sentiment, aggregation, attention detection"
license = {text = "MIT"}
requires-python = ">=3.8"
